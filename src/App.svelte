<script>
  // import { patients, samples, addPatient, addSample, scheduleSample } from './stores.js';
  // import PatientForm from './PatientForm.svelte';
  // import SampleForm from './SampleForm.svelte';
  // import Schedule from './Schedule.svelte';

  // let showAddPatient = false;
  // let showAddSample = false;

</script>

<h1>Lab Sample Scheduler</h1>

{/* <button on:click={() => showAddPatient = !showAddPatient}>
    {showAddPatient ? 'Close Add Patient' : 'Add Patient'}
</button>
{#if showAddPatient}
    <PatientForm />
{/if}

<button on:click={() => showAddSample = !showAddSample}>
    {showAddSample ? 'Close Add Sample' : 'Add Sample'}
</button>
{#if showAddSample}
  <SampleForm />
{/if}

<Schedule />

<h2>Patients</h2>
<ul>
  {#each $patients as patient}
    <li>{patient.name} ({patient.dob})</li>
  {/each}
</ul>

<h2>Samples</h2>
<ul>
  {#each $samples as sample}
    <li>{sample.type} - Scheduled: {sample.scheduledTime || 'Not Scheduled'}</li>
  {/each}
</ul> */}
